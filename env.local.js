// Committed placeholder so GitHub Pages does not return 404 for this script.
// CI injects the API key in index.html before this file runs.
// For local dev without deploy, uncomment:
// window.__API_KEY__ = 'YOUR_GOOGLE_API_KEY';
