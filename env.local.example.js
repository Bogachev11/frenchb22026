// Shipped to GitHub Pages as env.local.js (see deploy.yml). Does not set a key;
// the API key is injected into index.html in CI.
// Local: run `npm run env:local` with GOOGLE_API_KEY set — overwrites env.local.js (gitignored).
